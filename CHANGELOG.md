# Changelog

## v0.1

- Unity EditorWindow：`Tools/Codex`
- 使用 `cmd.exe` 将 Codex 输出重定向到文件，并在 Unity 侧轮询读取（Domain Reload 安全）
- `Library/CodexUnity/` 持久化：`state.json` / `history.jsonl` / `runs/<runId>/...`
- 基础操作：Send / New Task / Kill 进程树 / 打开 run 目录 / 复制命令

